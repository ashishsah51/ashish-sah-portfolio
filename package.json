{
  "name": "@caffeine/template-app",
  "type": "module",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "npm run dev --prefix src/frontend",
    "build": "npm run build --prefix src/frontend",
    "typecheck": "npm run typecheck --prefix src/frontend",
    "check": "npm run check --prefix src/frontend",
    "fix": "npm run fix --prefix src/frontend"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  }
}
